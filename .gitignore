# build output
dist/
# Claude Design 基座包(由 scripts/gen-dsbundle.mjs 从 global.css 生成,可重建,不进仓库)
ds-bundle/
# generated types
.astro/
# deps
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# env
.env
.env.production
# raw intake / source material (原始投稿资料:xlsx、原图等,不进仓库)
assets/
# local-only docs (本地保留,不进仓库):编辑手册 + 给 agent 的架构地图
EDITING.md
context.md
# os
.DS_Store
