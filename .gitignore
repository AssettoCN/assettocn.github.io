# build output
dist/
# Claude Design 相关(设计工具的产物与配置,不属于静态站源码,本地保留即可):
#   .design-sync/ —— 同步配置(projectId)、约定说明,以及生成器 gen-dsbundle.mjs
#   ds-bundle/    —— 基座包,由该生成器从 global.css 重新生成,可重建
ds-bundle/
.design-sync/
# generated types
.astro/
# 本地缓存(scripts/gen-og.mjs 下载的 ttf + fontconfig 缓存,可重下)
.cache/
# deps
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# env
.env
.env.production
# raw intake / source material (原始投稿资料:xlsx、原图等,不进仓库)
assets/
# local-only docs (本地保留,不进仓库):编辑手册 + 给 agent 的架构地图
EDITING.md
context.md
# os
.DS_Store
