---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ServersPage from '../../components/pages/ServersPage.astro';
import { UI } from '../../data/ui.js';

const lang = 'zh';
const ui = UI[lang];
---
<BaseLayout lang={lang} section="servers" path="/servers" title={ui.nav.servers} description={ui.serversPage.sub}>
  <ServersPage lang={lang} />
</BaseLayout>
