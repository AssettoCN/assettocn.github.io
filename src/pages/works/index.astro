---
import BaseLayout from '../../layouts/BaseLayout.astro';
import WorksPage from '../../components/pages/WorksPage.astro';
import { UI } from '../../data/ui.js';

const lang = 'zh';
const ui = UI[lang];
---
<BaseLayout lang={lang} section="works" path="/works" title={ui.nav.works} description={ui.worksPage.sub}>
  <WorksPage lang={lang} />
</BaseLayout>
